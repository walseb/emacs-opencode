#+TITLE: Emacs OpenCode TODO

* TODO Surface =tui.toast.show= events
The OpenCode server forwards every bus event over the SSE stream
via =Bus.subscribeAll=, including =tui.toast.show= events used by the
MCP auth flow and any external caller of =POST /tui/toast=.  We
currently ignore them.  Surface in the echo area at minimum, with
a face based on the variant (info/success/warning/error).

* TODO Handle =installation.update-available= events
The TUI prompts the user via a confirm dialog and runs an upgrade.
At minimum we should =message= the user; eventually a customizable
hook so users can wire it to whatever update flow they prefer.

* DONE Fix issue where you can't scroll around when a response is streaming in

* TODO Handle emphasis (=**thing**=) in font-lock

* IN PROGRESS completion-at-point in session buffer input
- slash commands (done)
- @agent (done)
- file paths, once @file is supported
- completion providers should be pluggable so new triggers (@agent, @file, !shell) can be added
- slash completion only triggers when the input begins with optional whitespace followed by "/"

* TODO Support @file mentions (inlines file contents into prompt)

* TODO Fix unclear wording for permission requests
E.g. "OpenCode wants to rg foobar .*" or "OpenCode wants to foo/bar/baz.txt".
What do those mean?

* TODO Overview buffers (sessions/projects)
- List-returning functions for sessions/projects.
- Thin rendering buffer(s) + completion integration.

* TODO Session management commands
- Load session, fork, delete, rename.

* TODO Add newline before "block level" message parts if they are the first part of the message and there isn't a blank line above

* Archive
** DONE Address poor performance of SSE buffer/event dispatch
CLOSED: [2026-04-15 Wed]
- Only JSON-parse events with registered handlers
- Skip processing events without registered handlers entirely
- Made handler dispatch run sync by default instead of queueing via run-at-time 0
- Switched to url-retrieve request.el backend for HTTP calls
- Added a JS bridge (=opencode-sse-bridge.js=, run under bun/node) that
  parses SSE, filters by registered event types, and strips known-large
  fields, emitting one JSON line per event to Emacs.

** DONE Make it so toggling open a shell block output doesn't auto-close when new shell output streams in
CLOSED: [2026-03-25 Wed]

** DONE Render markdown tables with aligned columns
CLOSED: [2026-04-02 Thu]
Rewrite table text at render time: detect consecutive =|...|= lines, measure
max column widths, and pad cells.

** DONE Support @-mentioning agents
CLOSED: [2026-03-04 Wed]

** DONE Add tests
CLOSED: [2026-03-04 Wed 10:01]

** DONE Support !shell command mentions (inlines shell command output into prompt)
CLOSED: [2026-03-04 Wed]

** DONE Font lock in session buffers
CLOSED: [2026-02-25 Tue]
Should syntax highlight markdown and display diffs from the patch tool with
diff syntax highlighting.

** DONE Add support for managing LLM providers/connections
CLOSED: [2026-02-18 Wed 17:37]

** DONE Support showing reasoning blocks
CLOSED: [2026-02-19 Thu 20:38]

** DONE Display tool call errors in the session buffer
CLOSED: [2026-02-05 Thu 11:46]

** DONE Support slash commands in the session buffer
CLOSED: [2026-02-03 Tue 16:46]

** DONE Show token usage metadata in session buffer
CLOSED: [2026-01-31 Sat 00:45]

** DONE Show shell command being run in shell tool call
CLOSED: [2026-01-30 Fri 16:52]

** DONE Sometimes multiple "Read" tool calls end up on the same line
CLOSED: [2026-01-30 Fri 13:42]

** DONE Handle diff in edit tool calls
CLOSED: [2026-01-30 Fri 16:35]
- Show the diff after each edit like the TUI

** DONE Make the session buffer look pretty
CLOSED: [2026-01-30 Fri 15:12]
With colors!

** DONE Handle sub-agents/sub-sessions
CLOSED: [2026-01-30 Fri 13:13]

** DONE Buffer-aware commands
CLOSED: [2026-01-30 Fri 13:12]
*** DONE Ask about region/point/file context
CLOSED: [2026-01-29 Thu 17:04]
*** DONE Apply edits to selection or buffer
CLOSED: [2026-01-30 Fri 13:12]
*** DONE Send context to existing session buffers, not just new ones
CLOSED: [2026-01-30 Fri 13:12]

** DONE Make open-session reuse the existing session buffer if there is one
CLOSED: [2026-01-30 Fri 12:45]

** DONE Give connections a unique port so we can have multiple open connections
CLOSED: [2026-01-30 Fri 12:41]

** DONE Emacs tool integration (MCP)
CLOSED: [2026-01-30 Fri 01:08]
- Expose Emacs access to OpenCode tools.

** DONE Handle question functionality
CLOSED: [2026-01-29 Thu 14:19]
- =question.asked= SSE
- Prompts the user in Emacs via =completing-read=, with an option to type
  your own answer.

** DONE Handle permission requests
CLOSED: [2026-01-29 Thu 10:39]

** DONE Add command to send interrupt to opencode
CLOSED: [2026-01-29 Thu 00:15]

** DONE Vertical Slice (Launch → Chat → Stream)
CLOSED: [2026-01-28 Wed]
Initial milestone: spawn =opencode serve=, implement minimal HTTP client
(health, session create, prompt async), connect to =/event= SSE stream,
define =opencode-session-mode=, implement =M-x opencode= and shutdown.

** DONE Handle todo list functionality
CLOSED: [2026-01-28 Wed 22:40]
- =todo.updated= SSE renders the todo list with done items checked off.

** DONE SSE event handlers API
CLOSED: [2026-01-28 Wed 10:54]
- Handler registry for event types; dispatch SSE events to named Emacs hooks.

** DONE Auto-reload edited buffers
CLOSED: [2026-01-28 Wed 12:53]
- Global handlers for =file.edited= and =file.watcher.updated=; revert
  visiting buffers when their file is updated.

** DONE Render tool parts
CLOSED: [2026-01-28 Wed 11:10]
- Render message parts with type "tool" in the session buffer (name, status,
  output summary).

** DONE Agent selection + header indicator
CLOSED: [2026-01-28 Wed 12:53]
- Track available agents on connection; buffer-local agent state in session
  mode (select + cycle commands); show active agent in session header; send
  agent name with prompt requests.
